import SectionPlaceholder from "@/components/ui/SectionPlaceholder";

/** index-v2: kapanış CTA bandı — teklif hunisine çıkış (radial red glow imza pattern'i) */
export default function CtaSection() {
  return (
    <SectionPlaceholder
      eyebrow="Teklif Al"
      title="Projeniz için 24 saatte teklif"
      note="Çok adımlı teklif formu CTA bandı porte edilecek (model → m² → tarih → şehir; leads tablosuna düşer)."
      dark
    />
  );
}
