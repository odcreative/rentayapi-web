import SectionPlaceholder from "@/components/ui/SectionPlaceholder";

/** index-v2: "Process" — keşif → teklif → kurulum süreci (outlined numbers + kesik çizgi connector) */
export default function ProcessSection() {
  return (
    <SectionPlaceholder
      eyebrow="Süreç"
      title="Keşiften kuruluma 4 adım"
      note="İmza pattern'lerle (outlined numbers, kesik çizgi connector) süreç bandı porte edilecek."
    />
  );
}
